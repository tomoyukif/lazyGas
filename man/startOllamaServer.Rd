% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ollama-server.R
\name{startOllamaServer}
\alias{startOllamaServer}
\title{Start the Ollama server as a background process}
\usage{
startOllamaServer(base_url = NULL, wait_seconds = 45, log_file = NULL)
}
\arguments{
\item{base_url}{Ollama base URL.}

\item{wait_seconds}{Seconds to wait for the HTTP API to become ready.}

\item{log_file}{Optional log file path (temporary file by default).}
}
\value{
Invisibly, a list with \code{pid}, \code{base_url}, \code{log_file},
and \code{already_running}.
}
\description{
Spawns \code{ollama serve} in a separate process (unless a server is already
reachable at \code{base_url}). The process PID is recorded so that
\code{\link[=stopOllamaServer]{stopOllamaServer()}} can stop only servers started from this R session.
}
\details{
Requires the \code{ollama} CLI on \code{PATH}, or an Apptainer wrapper from
\code{\link[=configureLazyGasOllama]{configureLazyGasOllama()}} (official lazyGas setup).
}
