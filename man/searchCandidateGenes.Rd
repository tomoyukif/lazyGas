% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/search_candidate_genes.R
\name{searchCandidateGenes}
\alias{searchCandidateGenes}
\title{Search candidate genes by functional annotation}
\usage{
searchCandidateGenes(
  candidate = NULL,
  object = NULL,
  pheno = NULL,
  query,
  ann_cols,
  mode = c("keyword", "semantic", "both"),
  keyword_match = c("all", "any"),
  ignore.case = TRUE,
  min_score = 0.1,
  top_n = NULL,
  dedupe_genes = TRUE,
  n_topics = NULL
)
}
\arguments{
\item{candidate}{A data.frame of candidate genes (e.g. from
\code{\link[=lazyData]{lazyData()}} with \code{dataset = "candidate"}). Used when \code{object} is
\code{NULL}.}

\item{object}{A \code{LazyGas} object. When provided, the candidate list is
read with \code{\link[=lazyData]{lazyData()}} (\code{dataset = "candidate"}). \code{candidate} is
ignored in that case.}

\item{pheno}{Phenotype name or index passed to \code{\link[=lazyData]{lazyData()}} when
\code{object} is given. Required if \code{object} is not \code{NULL}.}

\item{query}{Character string describing the phenotype or biological process
of interest (space-separated keywords are allowed).}

\item{ann_cols}{Character vector of column names in \code{candidate} that hold
functional annotation text (e.g. GO terms, gene descriptions). If \code{NULL}
(default), all columns except peak/gene coordinates and SnpEff impact
counts are used (i.e. columns joined from \code{ann} in \code{\link[=listCandidate]{listCandidate()}}).}

\item{mode}{Search mode: \code{"keyword"} (fixed substring match),
\code{"semantic"} (text2vec LSA cosine similarity), or \code{"both"}.}

\item{keyword_match}{For keyword mode, \code{"all"} requires every query token to
appear in the annotation text; \code{"any"} requires at least one token.}

\item{ignore.case}{Passed to \code{\link[base:grepl]{grepl()}} for keyword matching.}

\item{min_score}{Minimum cosine similarity for semantic matches (0--1).}

\item{top_n}{If not \code{NULL}, return at most this many rows after ranking by
\code{match_score}.}

\item{dedupe_genes}{If \code{TRUE} and \code{Gene_ID} is present, keep one row per
gene with the highest \code{match_score}.}

\item{n_topics}{Number of LSA topics. Default uses up to 30 topics bounded by
corpus size.}
}
\value{
A subset of the input candidate \code{data.frame} with columns
\code{keyword_score}, \code{semantic_score}, \code{match_score}, and \code{match_method}
added, sorted by decreasing \code{match_score}.
}
\details{
Semantic search requires the suggested package \pkg{text2vec}:
\code{install.packages("text2vec")}.

Annotation text for each row is the space-separated concatenation of
non-empty values in \code{ann_cols}.
}
\description{
Filter and rank genes in a candidate list using keyword matching and/or
semantic similarity (offline LSA via the \pkg{text2vec} package).
}
\examples{
cand <- data.frame(
  peak_ID = 1L,
  Gene_ID = c("g1", "g2", "g3"),
  Gene_chr = "1",
  Gene_start = 1:3,
  dist2peak = 0,
  negLog10P = 3,
  Description = c(
    "fruit weight development",
    "root hair elongation",
    "cell wall biosynthesis"
  ),
  stringsAsFactors = FALSE
)
searchCandidateGenes(
  candidate = cand,
  query = "fruit weight",
  mode = "keyword",
  keyword_match = "all"
)
}
\seealso{
\code{\link[=lazyData]{lazyData()}}, \code{\link[=listCandidate]{listCandidate()}}
}
