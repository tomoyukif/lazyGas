% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shiny-runner.R
\name{runLazyGasRunner}
\alias{runLazyGasRunner}
\title{Launch the lazyGas pipeline runner (Shiny)}
\usage{
runLazyGasRunner(gds_path = NULL, ...)
}
\arguments{
\item{gds_path}{Optional default GDS path pre-filled in the Shiny UI.}

\item{...}{Arguments passed to \code{shiny::runApp()}.}
}
\description{
Opens a lightweight Shiny application to run the core GWAS pipeline
(\code{buildLazyGas()} → \code{assignPheno()} → \code{runLazyGas()}) from
filesystem paths. Use \code{\link[=runLazyGasExplorer]{runLazyGasExplorer()}} afterward to explore and rank
candidates.
}
\details{
Requires the Suggested package \code{shiny}. Phenotype CSV must contain a
sample id column named \code{id} or \code{ID}. The companion store
(\code{{stem}.lazygas/}) is created next to the GDS file.
}
\seealso{
\code{\link[=runLazyGas]{runLazyGas()}}, \code{\link[=runLazyGasExplorer]{runLazyGasExplorer()}}, \code{\link[=runExplorerDemo]{runExplorerDemo()}}
}
