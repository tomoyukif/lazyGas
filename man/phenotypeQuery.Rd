% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/phenotype-query.R
\name{phenotypeQuery}
\alias{phenotypeQuery}
\title{Build a structured phenotype exploration query}
\usage{
phenotypeQuery(
  text,
  tissues = NULL,
  stage = NULL,
  conditions = NULL,
  species = NULL,
  use_llm = FALSE,
  llm_model = NULL,
  llm_base_url = NULL,
  object = NULL,
  save = !is.null(object)
)
}
\arguments{
\item{text}{Character string describing the phenotype of interest.}

\item{tissues}{Character vector of tissues or organs (optional).}

\item{stage}{Character vector of developmental stages (optional).}

\item{conditions}{Character vector of environmental or experimental
conditions (optional).}

\item{species}{Target species name (optional).}

\item{use_llm}{If \code{TRUE}, attempt LLM-based structuring via
\code{\link[=llmChat]{llmChat()}}.}

\item{llm_model}{Model name passed to Ollama (default from
\code{LAZYGAS_LLM_MODEL} or \code{"llama3.2:3b"}).}

\item{llm_base_url}{Ollama base URL (default from \code{LAZYGAS_LLM_URL}).}

\item{object}{Optional \code{LazyGas} object; when \code{save = TRUE} the
query is written to the companion store.}

\item{save}{Save the query JSON to the companion store when \code{object} is
given.}
}
\value{
A \code{PhenotypeQuery} object (list with class attribute).
}
\description{
Parses free text (and optional tissue/stage/condition fields) into a
\code{PhenotypeQuery} object used by \code{\link[=rankPhenotypeCandidates]{rankPhenotypeCandidates()}} and
\code{\link[=collectGeneEvidence]{collectGeneEvidence()}}. When \code{use_llm = TRUE} and a local Ollama
server is reachable, the query is structured via JSON output from the LLM;
otherwise a deterministic token-based parser is used.
}
\examples{
q <- phenotypeQuery(
  "fruit weight at maturity",
  tissues = "fruit",
  stage = "maturation",
  use_llm = FALSE
)
q$trait_keywords
}
\seealso{
\code{\link[=rankPhenotypeCandidates]{rankPhenotypeCandidates()}}, \code{\link[=explainPhenotypeCandidates]{explainPhenotypeCandidates()}}
}
