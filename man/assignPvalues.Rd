% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pheno.R
\name{assignPvalues}
\alias{assignPvalues}
\title{Register externally computed association results in the companion store}
\usage{
assignPvalues(
  object,
  pheno_name,
  p_values,
  coef = NULL,
  any_data = NULL,
  geno_format = c("genotype", "dosage", "haplotype"),
  conv_fun = NULL,
  formula = "phe ~ add",
  null_formula = NULL,
  kruskal = NULL,
  ...
)
}
\arguments{
\item{object}{A \code{LazyGas} object.}

\item{pheno_name}{Character vector of phenotype name(s).}

\item{p_values}{Numeric vector, matrix, or data.frame. Rows must equal
\code{nmar(object)}. A vector is \code{P.model} for one phenotype; matrix
columns map to \code{pheno_name} in order. \code{FDR} and \code{negLog10P}
are derived from \code{P.model}.}

\item{coef}{Optional effect sizes to store with the scan. A numeric vector
(length \code{nmar}) becomes \code{Coef.add}. A matrix or \code{data.frame}
with \code{nrow = nmar} uses its column names (e.g. \code{Coef.add},
\code{Coef.dom}). When \code{p_values} has multiple columns, \code{coef}
may have the same number of columns (one per phenotype) or a single
coefficient block shared across phenotypes.}

\item{any_data}{Optional \code{data.frame} with \code{nrow = nmar}. Its
columns are appended to the stored scan table (e.g. external test
statistics). Column names must not duplicate \code{P.model}, \code{FDR},
\code{negLog10P}, or \code{coef} columns.}

\item{geno_format}{Genotype format metadata for downstream steps.}

\item{conv_fun, formula, null_formula, kruskal}{Scan metadata recorded for
peak calling and recalculation.}

\item{...}{Unused.}
}
\value{
The updated \code{LazyGas} object (invisibly).
}
\description{
Use this when p-values (and optionally coefficients or other per-marker
columns) come from another GWAS tool, not from \code{\link[=scanAssoc]{scanAssoc()}}. Each call
writes a \strong{new} scan table for \code{pheno_name}; it does not merge into
an existing \code{scanAssoc()} result. To keep downstream plots such as
\code{\link[=haploPlot]{haploPlot()}} informative, pass \code{coef} and/or \code{any_data} together
with \code{p_values}.
}
\seealso{
\code{\link[=scanAssoc]{scanAssoc()}}, \code{\link[=lazyData]{lazyData()}}
}
