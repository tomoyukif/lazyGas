% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scan.R
\name{getGenoPerMarker}
\alias{getGenoPerMarker}
\title{Extract genotype values for a single marker}
\usage{
getGenoPerMarker(object, geno_format, marker_index = 1L, ...)
}
\arguments{
\item{object}{A \code{LazyGas} object.}

\item{geno_format}{One of \code{"genotype"}, \code{"corrected"},
\code{"dosage"}, or \code{"haplotype"}.}

\item{marker_index}{Integer index of the marker among
\code{validMar(object)} markers (default \code{1L}).}

\item{...}{Unused. Legacy \code{marker_id=} is accepted with a warning and
treated as \code{marker_index}.}
}
\value{
\describe{
\item{dosage}{Integer vector of length \code{nsam(object)}.}
\item{genotype / haplotype}{Matrix with dimensions
\code{c(ploidy, nsam)} (rows = alleles, columns = samples).}
}
}
\description{
Returns genotypes for one marker. Among valid markers,
\code{marker_index} is a 1-based index (not a variant ID).
}
