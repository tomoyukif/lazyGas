% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tenor.R
\name{downloadTenorExpression}
\alias{downloadTenorExpression}
\title{Download TENOR expression profiles from RAP-DB for gene IDs}
\usage{
downloadTenorExpression(
  gene_ids,
  object = NULL,
  out_dir = NULL,
  datasets = NULL,
  primary_transcript_only = TRUE,
  overwrite = FALSE,
  sleep_sec = 0.15,
  timeout = 60,
  quiet = FALSE
)
}
\arguments{
\item{gene_ids}{Character vector of RAP gene / transcript IDs
(e.g. \code{Os01g0911700} or \code{Os01t0911700-01}).}

\item{object}{Optional \code{LazyGas} object for default cache location.}

\item{out_dir}{Cache directory (see \code{\link[=tenorCacheDir]{tenorCacheDir()}}).}

\item{datasets}{Optional data.frame with \code{dataset} and
\code{publication} columns. Default: \code{\link[=tenorDatasetCatalog]{tenorDatasetCatalog()}}.}

\item{primary_transcript_only}{If \code{TRUE}, download only the first
transcript per locus (typically \code{*-01}).}

\item{overwrite}{Re-download even if CSV already exists.}

\item{sleep_sec}{Pause between API calls (be polite to RAP-DB).}

\item{timeout}{HTTP timeout in seconds.}

\item{quiet}{Suppress progress messages.}
}
\value{
A data.frame summarizing download status per gene
(\code{gene_id}, \code{locus_id}, \code{transcript_id}, \code{path},
\code{status}, \code{n_rows}).
}
\description{
For each gene ID, resolves RAP-DB locus/transcripts via
\code{/tools/Feature}, then queries \code{/api/rnaseq} for each TENOR
dataset. Results are written to one CSV per gene under
\code{lazygas/ai_report/tenor_download/} with columns
\code{data_description} and \code{tpm}. Non-TPM fields are joined with
\code{":"} into \code{data_description}. Existing files are skipped unless
\code{overwrite = TRUE}.
}
\details{
Bulk matrix download is not provided by RAP-DB; this per-gene API is the
supported Phase 1 path for candidate-region testing.
}
\examples{
\dontrun{
downloadTenorExpression("Os01g0911700")
}
}
