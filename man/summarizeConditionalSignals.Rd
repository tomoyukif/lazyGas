% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/conditional.R
\name{summarizeConditionalSignals}
\alias{summarizeConditionalSignals}
\title{Summarize independent signals from conditional association}
\usage{
summarizeConditionalSignals(
  object,
  pheno = NULL,
  peak_id = NULL,
  p_threshold = 0.05,
  r2_threshold = 0.8,
  recalc = TRUE,
  store = FALSE
)
}
\arguments{
\item{object}{A \code{LazyGas} object, or output from \code{\link[=conditionalAssoc]{conditionalAssoc()}}.}

\item{pheno, peak_id}{Required when \code{object} is a \code{LazyGas} object.}

\item{p_threshold}{P-value threshold for calling an independent lead.}

\item{r2_threshold}{Passed to \code{\link[=assessIndependentSignals]{assessIndependentSignals()}} when \code{object}
is a \code{LazyGas} object.}

\item{recalc}{Use recalculated peak blocks when running \code{\link[=conditionalAssoc]{conditionalAssoc()}}.}

\item{store}{If \code{TRUE}, write the summary row to the companion store.}
}
\description{
Counts stepwise leads and, when a \code{LazyGas} object is supplied, classifies
them with \code{\link[=assessIndependentSignals]{assessIndependentSignals()}} so that \code{n_independent_signals}
reflects LD-supported signals rather than raw stepwise picks.
}
