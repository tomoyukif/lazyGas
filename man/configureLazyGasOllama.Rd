% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ollama-apptainer.R
\name{configureLazyGasOllama}
\alias{configureLazyGasOllama}
\title{Configure lazyGas to use Ollama inside Apptainer (official setup)}
\usage{
configureLazyGasOllama(
  sif = NULL,
  home_dir = NULL,
  apptainer_bin = NULL,
  base_url = "http://127.0.0.1:11434",
  model = "llama3.2:3b",
  build_sif_if_missing = TRUE
)
}
\arguments{
\item{sif}{Path to the Ollama \code{.sif} image. Built via \code{\link[=buildLazyGasOllamaSif]{buildLazyGasOllamaSif()}}
when missing and \code{build_sif_if_missing = TRUE}.}

\item{home_dir}{Host directory bound to \code{/root/.ollama} (models/cache).}

\item{apptainer_bin}{Path to \code{apptainer} or \code{singularity}.}

\item{base_url}{HTTP URL for the Ollama API (default \code{http://127.0.0.1:11434}).}

\item{model}{Default model name (\code{LAZYGAS_LLM_MODEL}).}

\item{build_sif_if_missing}{Run \code{\link[=buildLazyGasOllamaSif]{buildLazyGasOllamaSif()}} when \code{sif} is absent.}
}
\value{
Invisibly, a list with \code{wrapper}, \code{sif}, \code{home_dir},
and \code{base_url}.
}
\description{
Installs a wrapper script (\code{lazygas-ollama}) and sets environment variables
so \code{\link[=startOllamaServer]{startOllamaServer()}}, \code{\link[=pullOllamaModel]{pullOllamaModel()}}, and the Shiny explorer use the
containerised Ollama CLI instead of a host installation.
}
\seealso{
\code{\link[=buildLazyGasOllamaSif]{buildLazyGasOllamaSif()}}, \code{\link[=startLocalLLM]{startLocalLLM()}}, \code{\link[=describeOllamaStatus]{describeOllamaStatus()}}
}
